name: CI

on:
  push:
  pull_request:
  schedule:
    - cron: '42 * * * *'
  workflow_dispatch:

jobs:
  build-in-container:
    runs-on: ubuntu-latest
    container:
      image: python:3.7
    name: Run
    steps:
      - uses: actions/checkout@v4

      - name: Monitor
        run: |
            uv run monitor.py config/*.json config/*.yaml
